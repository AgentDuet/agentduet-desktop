# people/

One file per external party the owner has told the agent about: `<identity>.md`.

Empty on a fresh install. A profile holds what only THIS person may be told, plus per-person
rules (`## Folders` for access only they get, `## Always escalate` for topics to route to the
owner however readable the source). Anything everyone may hear belongs in `knowledge/` instead.
