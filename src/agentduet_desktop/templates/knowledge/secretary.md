# How this assistant works

What an external party may need to know about the arrangement itself. Deliberately NOT the
agent's rules: it knows those from its own instruction, and restating them here put the same
rule in two places — where the copy is quotable but unenforced. Verified by removing this file:
the agent still refused to agree a price and still explained that it answers only from the
documents it has.

- Messages are answered by an assistant acting for the owner, not by the owner.
- The owner can see what was asked and what was answered.
